# Copy this to .env and fill in real values
# VPS — frps (server side)

# Shared secret between frps and frpc — use a long random string
FRP_TOKEN=change_me_use_a_long_random_string

# Dashboard password (optional, set to anything if you disable the dashboard)
DASHBOARD_PWD=change_me_dashboard_password
